# Python venv (legacy)
.venv/

# Python bytecode
__pycache__/
*.pyc

# JS / Node
node_modules/
build/

# Secrets + credentials (never commit these)
client_secret.json
token.json
oauth.json
ytm_client.json
YTMusic_secret.json
.env

# Editor / OS
backup/
cache/
.vscode/
.DS_Store
Thumbs.db

# Notes
notes/

# Chrome extension .pem key (used for pinning extension ID — keep this private)
*.pem
