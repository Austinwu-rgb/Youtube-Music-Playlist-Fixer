{
  "name": "youtube-music-replacer",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that finds and replaces unplayable YouTube Music playlist tracks",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "preview": "vite build && echo 'Load the build/ folder as an unpacked extension in chrome://extensions'"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0",
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5",
    "vite": "^5.4.0"
  }
}
